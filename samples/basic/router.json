{
  "apiVersion": "kimchi/v1",
  "kind": "Router",
  "metadata": {
    "name": "basic-router"
  },
  "spec": {
    "rules": [
      {
        "path": "/test",
        "pathType": "Exact",
        "methods": ["GET"]
      }
    ],
    "config": {
      "destinations": [
        {
          "destinationRef": { "kind": "Service", "name": "backend" },
          "weight": 100
        }
      ]
    }
  }
}
